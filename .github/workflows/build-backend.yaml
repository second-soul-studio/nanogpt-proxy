name: Build & Test Backend (nanogpt-proxy)

on:
  push:
    paths:
      - 'proxy-js-v1/**'
  pull_request:
    paths:
      - 'proxy-js-v1/**'

jobs:
  test-backend:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: proxy-js-v1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: proxy-js-v1/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: proxy-js-v1

      - name: Run tests with coverage
        run: NODE_OPTIONS=--experimental-vm-modules npx jest --coverage
        working-directory: proxy-js-v1

      - name: Upload Coverage Report (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-backend
          path: proxy-js-v1/coverage
